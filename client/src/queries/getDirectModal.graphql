query getDirectModal($userId: Float!) {
  getUser(userId: $userId) {
    id
    username
  }
  me {
    id
    username
    teams {
      id
      name
      admin
      channels {
        id
        isPublic
        name
      }
      members {
        username
        id
      }
    }
  }
}
