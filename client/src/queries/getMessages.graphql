query getMessages($channelId: Float!) {
  getMessages(channelId: $channelId) {
    id
    text
    user {
      id
      username
    }
    file {
      id
      filename
      mimetype
      url
    }
    createdAt
    updatedAt
  }
}
