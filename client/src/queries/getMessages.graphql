query getMessages($channelId: Float!, $offset: Float) {
  getMessages(channelId: $channelId, offset: $offset) {
    id
    text
    user {
      id
      username
    }
    file {
      id
      filename
      mimetype
      url
    }
    createdAt
    updatedAt
  }
}
