mutation createTeam($createTeamInput: CreateTeamInput!) {
  createTeam(createTeamInput: $createTeamInput) {
    ok
    team {
      id
      name
      owner {
        username
      }
      channels {
        id
        isPublic
        name
      }
    }
    errors {
      msg
      path
    }
  }
}
