# Slack clone with typescript

Server build with `apollo`, `type-graphql` and `typeorm`. Client using `react` with `apollo`

## Server

- `typeorm`[link](https://typeorm.io) configured to work with **postgres** db
